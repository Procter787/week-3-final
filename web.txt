//The internet is a interconnect network of computers that hold information. It is a very very complicated system of networks. All of these computers can comminicate with eachother to communicate information. Devices called "routers" help reroute the information so that the network does not get slowed down.
//you initially click the link and the computer starts to compute what function you want to be carried out. your request will be carried out by the app server. the computer will process and run through the code that is linked to the link. the HTML will be processed and the page will then be rendered in your browser. 